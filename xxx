import os
import glob

def get_newest_file(dir_path):
    # Get list of all files in the directory
    list_of_files = glob.glob(os.path.join(dir_path, '*'))
    # Find the file with the latest modification time
    newest_file = max(list_of_files, key=os.path.getmtime)
    return newest_file

# Example usage
dir_path = '/Users/YOURUSERNAME/DBID_Automation/Report'
print(get_newest_file(dir_path))
